/* 
 * File:   Matrix 1D.h
 * Author: antoinenouma
 *
 * Created on 14 mars 2016, 08:32
 */

/*#include "matrix.h"

struct matrix1D {struct matrixHeader header; int *data;};

typedef struct matrix1D ts_matrix1D;

ts_matrix1D* m1d_create(const unsigned char number_of_row, const unsigned char number_of_column);

int m1d_get (const ts_matrix1D matrix, const unsigned char row, const unsigned char column);

void m1d_set (const ts_matrix1D matrix, const unsigned char row, const unsigned char column, const int value);

void m1d_print (const ts_matrix1D matrix);

ts_matrix1D * m1d_ascii_load (tp_file_name file_name);

int m1d_ascii_save (tp_file_name file_name, const ts_matrix1D matrix);

ts_matrix1D* m1d_binary_load (tp_file_name file_name);

int m1d_binary_save (tp_file_name file_name, const ts_matrix1D matrix);

ts_matrix1D* m1d_binary_block_load (tp_file_name file_name);

int m1d_binary_block_save (tp_file_name file_name, const ts_matrix1D matrix);

void m1d_free (ts_matrix1D * p_matrix);
*/